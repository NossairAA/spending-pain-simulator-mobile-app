# MindSpend Flutter App

This directory contains the Flutter mobile application for MindSpend.

For full documentation (setup, architecture, flows, and build instructions), see the repository root README:

- `../README.md`
